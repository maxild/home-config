  <!-- free Shift+Ctrl+Alt+F12 -->
  <action id="RestoreDefaultLayout"/>

  <!-- shift+alt+enter used to toogle full screen-->
  <action id="AddToFavoritesPopup"/>
  <!-- shift+alt+N used to show NuGet Quick List -->
  <action id="tasks.goto"/>
  <!-- shift+alt+L used by show in solution explorer -->
  <action id="context.load"/>
  <!-- free shift+alt+S  -->
  <action id="context.save"/>

  <!--Remove all Alt+Left and Alt+Right bindings -->
  <action id="PreviousTab"/>
  <action id="NextTab"/>

  <action id="PreviousEditorTab"/>
  <action id="NextEditorTab"/>

  <action id="Diff.PrevChange"/>
  <action id="Diff.NextChange"/>

  <!-- free control Enter that is used for Generate and NewElement -->
  <action id="ViewSource"/>

  <!-- free Ctrl+D -->
  <action id="CompareTwoFiles"/>

  <!-- free F5 -->
  <action id="CopyElement" />

  <!-- free shift+alt+B -->
  <action id="tasks.open.in.browser" />
  <!-- free shift+alt+T -->
  <action id="tasks.switch" />
  <!-- free shift+alt+W -->
  <action id="tasks.close" />

  <!-- free shift+alt+G -->
  <action id="EditorAddCaretPerSelectedLine" />

  <!-- free shift+alt+U -->
  <action id="ShelveChanges.UnshelveWithDialog"/>

  <!-- free ctrl+alt+T -->
  <action id="SurroundWith"/>

  <!-- free shift+alt+R -->
  <action id="RerunTests"/>

  <!-- free F10 ('step over in debug') -->
  <action id="SwitchHeaderSource"/>

  <!-- free ctrl+T -->
  <action id="Vcs.UpdateProject" />

  <!-- free ctrl+W -->
  <action id="UsageFiltering.WriteAccess " />
